{
  "name": "everyawesome-pipeline",
  "version": "0.1.0",
  "private": true,
  "description": "Markdown -> JSON parser for awesome-list aggregation",
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}
